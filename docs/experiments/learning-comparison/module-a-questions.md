# Module A - graph contract

Select one answer for each question.

1. Which project metadata field binds the graph to an exact analyzed source snapshot?

   - A. `analyzedAt`
   - B. `gitCommitHash`
   - C. `version`
   - D. `frameworks`

2. Which list correctly names the four structural collections on the root `KnowledgeGraph`?

   - A. `nodes`, `edges`, `layers`, `tour`
   - B. `files`, `imports`, `screens`, `lessons`
   - C. `project`, `packages`, `routes`, `receipts`
   - D. `types`, `schemas`, `prompts`, `videos`

3. What groups related graph nodes into architecture areas?

   - A. Edge weights
   - B. Project frameworks
   - C. Layers
   - D. File extensions

4. What supplies the graph's guided learning order?

   - A. Node complexity
   - B. Git history
   - C. Search results
   - D. Tour steps

Submit answers as four letters in order, for example: `B, A, C, D`.
