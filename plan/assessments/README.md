# External TEMPO assessment snapshots

These files are exact, byte-preserved copies of machine assessments produced
in the separate product-governance workspace. They are product provenance, not
a local TEMPO installation and not build authority.

`A-5D9CD4780724426B.json` is the current unsigned snapshot. It was produced by
TEMPO commit `4afc6a3` with machine preparation attributed to `agent:codex`.
Its assessment hash is
`sha256:6632cfa3dfbb69781af1bca737226fa1d6ec32b13bd536628fa54004b8f6b0e1`.
It records `E-HUMAN-MEDIA-DEFECTS-001` as contradictory evidence and exactly
two blockers: `READINESS_POLICY_UNSIGNED` and `DECISION_BRIEF_UNSIGNED`.

`A-24F38384E89B4594.json` is preserved as the immediately superseded snapshot
from before the media defects were split into a machine-visible contradictory
record.

Authority must be validated against the live external workspace; copying a
snapshot here cannot issue, extend, or replace a warrant.
